# Loaded automatically by Next.js for production builds (Vercel).
# The dashboard calls its own origin at /backend, which next.config.ts
# proxies to the VoxDesk API on the VPS. No Vercel dashboard env vars needed.
NEXT_PUBLIC_API_URL=/backend
